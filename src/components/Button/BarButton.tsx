import React from 'react';
import { Button } from 'react-native-elements';

const BarButton = (): JSX.Element => {
  const obj = {};
  console.log(obj?.a);
  return <Button />;
};

export default BarButton;
